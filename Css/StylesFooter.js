import { StyleSheet } from "react-native";

const sty = StyleSheet.create({
  container: {
    margin: 15,
    justifyContent: "center",
    alignItems: "center",
  },
  text: {
    marginBottom: 10,
    fontSize: 11,
  },
  link: {
    flexDirection: "row",
  },
});

export { sty };
